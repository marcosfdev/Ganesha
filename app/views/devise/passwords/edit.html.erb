  <section id="content" class="m-t-lg wrapper-md animated fadeInUp">
  <a class="nav-brand">Salon Objectives</a>
    <div class="row m-n">
      <div class="col-md-4 col-md-offset-4 m-t-lg">
        <section class="panel">
          <header class="panel-heading text-center">
           Change Your Password
          </header>
<%= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => {:method => :put , :class => 'panel-body' }) do |f| %>
 <div class="form-group">
  <%= f.hidden_field :reset_password_token %></div>
   <div class="form-group">
  <%= f.input :password, :required => true, :autofocus => true %></div>
   <div class="form-group">
  <%= f.input :password_confirmation, :required => true %></div>
  <%= f.button :submit, 'Change my password', :class => 'btn btn-info' %>
<% end %>
<%= render "devise/shared/links" %>

