  <section id="content" class="m-t-lg wrapper-md animated fadeInUp">
     <a class="nav-brand">Salon Objectives</a>
    <div class="row m-n">
      <div class="col-md-4 col-md-offset-4 m-t-lg">
        <section class="panel">
          <header class="panel-heading text-center">
           Forgot Your Password?
          </header>


<%= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => {:method => :post , :class => 'panel-body' }) do |f| %>
 <div class="form-group">
  <%= f.input :email, :required => true, :autofocus => true %></div>
  <%= f.button :submit, 'Send me reset password instructions', :class => 'btn btn-info' %>
<% end %>
<%= render "devise/shared/links" %>
          </form>
        </section>
      </div>
    </div>
  </section>
